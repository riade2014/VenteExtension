@model IEnumerable<VenteExtension.Models.Panier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.client.nomCl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.produit.NomExt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.commandeID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prixTotcli)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.client.nomCl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.produit.NomExt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.commandeID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prixTotcli)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.panierID }) |
            @Html.ActionLink("Details", "Details", new { id=item.panierID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.panierID })
        </td>
    </tr>
}

</table>
